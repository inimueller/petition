<p>These are the people who have trusted me: <br> ...</p>

{{#rows}}
<div id="allsigners">
 <p>
                    {{#if url}}
                        <a href="{{url}}" >{{first}} {{last}}</a>
                    {{else}}   
                        <a >{{first}} {{last}}</a>
                    {{/if}}
                 
                    {{#if age}}
                        <a>{{age}}</a>
                    {{/if}} 
                     {{#if city}}
                        <a href="/signers/{{city}}">{{city}}</a>
                    {{/if}} 
                </p> 
</div>
{{/rows}}